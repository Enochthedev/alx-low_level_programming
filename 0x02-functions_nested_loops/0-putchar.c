#include <stdio.h>
/**
 * printf - writes the character _putchar to stdout
 *
 * Return: On success 0
 */
main(void)
{
        char *pt = "_putchar";
                printf(pt,'%c');
                return(0);
}
